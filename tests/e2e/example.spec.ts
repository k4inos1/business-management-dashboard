import { test } from '@playwright/test';

test.describe('AI Agent Sandbox - Business Dashboard', () => {
  test('Should be able to load the dashboard', async () => {
    // Sandbox test for AI agents
    // await page.goto('http://localhost:5173/');
    
    // AI agents can use this to verify dashboard metrics or interact with charts
    // await expect(page).toHaveTitle(/Business Dashboard/);
  });
});
